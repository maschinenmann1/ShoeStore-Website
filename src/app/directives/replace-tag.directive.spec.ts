import { ReplaceTagDirective } from './replace-tag.directive';

describe('ReplaceTagDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceTagDirective();
    expect(directive).toBeTruthy();
  });
});
